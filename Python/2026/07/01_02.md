LLM曰く、mapとlistの複合などはパターンが少ないので呪文として覚えたほうがいいとのこと

```py
# 一行入力（整数）
n = int(input())

# 複数整数を変数に
a, b = map(int, input().split())

# 整数リスト
values = list(map(int, input().split()))
```

特に最後のやつを毎回間違える。
for+ifも実はまだ苦手意識がある。
forの前に＃のコメントをつけてなんの処理を繰り返すのか言語化してからが結局早い。
