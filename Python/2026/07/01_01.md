七月になりました。明日はインターンの面接。
試験で出たところを聞かれてもいいように復習しつつ、HoudiniとMayaのPythonをいじくります。

```py
a, b = input().split()
con = int(a)
num = int(b)
num_list = list(map(int,input().split()))

anser = 0

for i in range(num):
    anser = num_list[i]
    
print(anser)
```

やるだけマシではあるが、これではToday I Larnedとは言えず今日書いたコードを残すだけの習慣になっている気もする。
時間が空いたら基本的なfor文、mapなどの使い方からしっかりまとめたい所存
