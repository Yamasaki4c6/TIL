マークダウンでのTILを習得したい


```python
class Person:

  a, b = input().split()
  con = int(a)
  num = int(b)
  num_list = list(map(int,input().split()))

  anser = 0

  for i in range(num):
    anser = num_list[i]
    
  print(anser)

